# Handoff — 2026-07-09 (sesja: conversation_id + auto-run + mowa)

Branch: `spike/jarvis-local-runtime-check`. Wszystko **niezacommitowane** (working tree).
Daemon działa z kodu repo (cwd=repo). **DB trzyma czas UTC** (lokalny CEST = +2h — nie pomyl się jak ja). DB jest w trybie **WAL** — przy odczycie z zewnątrz rób `PRAGMA wal_checkpoint(FULL)` albo czytaj świeżym połączeniem.

## ✅ NAPRAWIONE + zweryfikowane

### 1. Bug „każda PTT = nowy chat" (conversation_id) — GŁÓWNY
- **Root cause:** tura głosowa anulowana (barge-in/echo, częste zaraz po restarcie) **rzuca** w `handle_text_input` (orchestrator → `TurnCancelledError`), więc `_start_voice_turn` nigdy nie zapisywał rolling `_voice_conversation_id` → następna PTT startowała z pustym = nowy chat. Gdy tura zaczynała się KOŃCZYĆ (finished), id się utrwalał → „potem gada normalnie".
- **Dowód:** log instrumentacji + baza — tury `cancelled` mają różne conv i NIE ma ich w logu; pierwsza `finished` ma `in=None→out=<id>`, kolejne `in=out=<id>`.
- **Fix (decyzja Ozzy'ego: JEDNA konwersacja z Jarvisem, zero sesji):** głos używa **jednej trwałej konwersacji** zapisanej w settings `voice.conversation_id`. `_resolve_voice_conversation_id()` + przepisany `_start_voice_turn` w `jarvis/daemon/app.py`. Przeżywa restart I anulowanie. Rolling całkowicie usunięty.
- **Testy:** `test_cancelled_voice_turn_keeps_one_durable_conversation`, `test_voice_conversation_id_is_durable_across_restart` + `test_voice_turns_roll_into_one_conversation` (test_tool_result_continuation.py).

### 2. Auto-run narzędzi (panel „wymagaj zgody" był martwy)
- Panel checkboxy `security.require_approval_for_{shell,file_write,network}` teraz **realnie** sterują (policy je czyta: `permissions.py` + `_class_requires_approval`). Odznaczone = ALLOW dla usera i modelu.
- Policy budowana **per-turę z efektywnych ustawień** (TOML ⊕ tabela `settings`, panel wygrywa) — `_policy_with_settings_overlay` + `_live_tool_permission_policy` w app.py. Zmiana w panelu działa od następnej tury bez restartu.
- **Dziura ALLOW→wykonaj** zamknięta: model tool z ALLOW auto-zatwierdza przez ApprovalGate + wykonuje + kontynuacja (`_auto_run_allowed_tools`). `destructive` dalej za jednym klikiem (bezpiecznik).
- Auto-run **kuloodporny** (`try/except`) — nie może wywalić tury ani zgubić conversation_id.
- Testy: test_effective_tool_policy.py, test_tool_permissions.py, test_tool_result_continuation.py.

### 3. Mowa `[[GŁOS]]` — piece 4 DOKOŃCZONY (sesja wieczorna 2026-07-09)
- (a) `SpeechFormStreamRouter` wpięty w orchestrator: `_speech_on_delta()` owija `speech_session.feed` per-tura w OBU torach (handle_text + kontynuacja po narzędziu). Router aktywny tylko gdy context builder prosi o blok — bez instrukcji delty idą wprost (inaczej live mowa milczałaby do finalize).
- (b) Instrukcja przerobiona: blok `[[GŁOS]]` **na początku** odpowiedzi (first-sound, opcja 2 Ozzy'ego), dokładnie jeden blok.
- (c) `_speech_form_enabled` → publiczne `speech_form_enabled()`, gate = `voice.enabled && speak_responses`.
- (d) Testy: `test_voice_form_instruction_present_when_speaking`, `test_speech_form_turn_speaks_only_the_glos_block` (markery/czat nigdy w kolejce TTS, pierwsze zdanie mowy w kolejce W TRAKCIE generacji), `test_speech_form_turn_without_block_degrades_to_final_text` (model zignorował blok → finalize mówi kanoniczny tekst, nigdy cisza).
- Degradacje: brak bloku = mowa dopiero po generacji (bez live streamu) — akceptowane; niedomknięty blok = router mówi wszystko od otwarcia (edge, patrz Minor #9 z review).

## 🔍 CODE REVIEW 2026-07-09 (agent, cała paczka niezacommitowana)
Werdykt: **With fixes**, zero Critical. Załatwione w tej sesji:
- **Important #4** ✅ pętla auto-run: wyjątek per-approval (malformed payload w allow-check) kontenerowany per iteracja — test `test_auto_run_skips_a_malformed_approval_and_still_runs_the_rest`.
- **Minor #5** ✅ docstring `_start_voice_turn` (opisywał usunięty rolling id), **#6** ✅ log `voice-turn conversation` PRZED turą (anulowane tury też logują; grep z kroku 1 teraz je widzi), **#7** ✅ martwy `_generate_speech_text` z claude_cli_adapter usunięty.
Domknięte w drugiej części sesji:
- **Important #1 ✅**: routes_runtime mówi prawdę — `LIVE_TOOL_POLICY_APPLY_KEYS` (6× require_approval + destructive_tools_enabled) są `apply_capable: True / requires_restart: False`, `/runtime/settings/apply` REALNIE je zapisuje do settings, projekcje `approval_required_tools`/`network_policy`/`destructive_tools_enabled` czytają live policy (overlay), nie TOML. Testy: `test_runtime_settings_tool_policy_keys_are_live_apply_capable`, `test_post_runtime_settings_apply_tool_policy_applies_live`.
- **Important #2 ✅ (decyzja Ozzy'ego)**: pełna kontrola z panelu ZOSTAJE i się rozszerza; dodatkowo walidacja enum `auto_approve_mode` (nieznany tryb → log + zostaje bazowy, test `test_unknown_auto_approve_mode_falls_back_to_off`).
Otwarte:
- **Important #3**: kontynuacja auto-run biegnie poza `text_turn_lock` przy stanie IDLE → okno na 2 równoległe generacje na warm adapterze. Do zaplanowania serializacja.
- **Minor #8** (approvals w wyniku tury dalej "pending" po auto-run = błysk w panelu), **#9** (multi-blok/niedomknięty `[[GŁOS]]` w split_display_and_speech), **#10** (luka testowa: toggle z powrotem na ON mid-sesja; zaostrzenie policy między approvalem a wykonaniem).

## 🔓 PANEL UPRAWNIEŃ + DEKRET „WSZYSTKO OTWARTE" (Ozzy, 2026-07-09 wieczór)
- **Nowe klasy przełączane**: `require_approval_for_ui` (ui_act), `require_approval_for_terminal` (terminal_write), `require_approval_for_memory` (memory_write — jawny override defaultu ADR-009 przez operatora) — permissions.py + SecurityConfig + overlay + apply. Unattended (scheduled/hook) NADAL blokowane niezależnie od grantów; destructive NADAL zawsze za klikiem.
- **Panel po nowemu (semantyka POZYTYWNA)**: sekcja „Jarvis może sam (bez pytania)" — 6 checkboxów grantów (zaznaczone = robi sam; zapis = `!checked` do require_approval_*) + „Destrukcyjne dozwolone (i tak zawsze pytają)" (`security.destructive_tools_enabled`). Stare 3 checkboxy „Wymagaj zgody" skasowane. Nowe idki: `grantShellToggle/grantFileWriteToggle/grantNetworkToggle/grantUiToggle/grantTerminalToggle/grantMemoryToggle/destructiveEnabledToggle`.
- **DEKRET: defaulty produktu OTWARTE** — `SecurityConfig`: wszystkie require_approval_* = False, destructive_tools_enabled = True (komentarz w config.py). `ToolPermissionPolicy` (klasa) świadomie zostaje fail-closed dla użycia bibliotecznego/testów; szablon configu testów (`write_config`) jawnie gate'uje wszystkie 6 klas, więc testy torów zgód działają jak dotąd. TOML Ozzy'ego już był otwarty (auto_approve all, roots=/Users/n1_ozzy); tabela settings NIE ma kluczy security.* (nic nie nadpisuje).
- **Root „3 dni blokad"**: to była dziura ALLOW→wykonaj (policy mówiła ALLOW, daemon i tak wieszał approval) — naprawiona dzisiejszym auto-runem; dekret defaultów domyka resztę.
- Test `test_app_settings_are_rendered_from_daemon_truth_only` zaktualizowany: localStorage może trzymać też `SELECTED_CONVERSATION_KEY` (feature Ozzy'ego — persystencja wybranej rozmowy). `test_tools_internet_controls...` przepisany na nowy kontrakt grantów.
- **Stan testów**: wszystkie moje suity zielone; jedyne czerwone = pre-existing potwierdzone stash-checkiem na czystym HEAD (5× test_api_smoke + 9× test_panel_assets — panel v4/mission-control drift, NIE z tej sesji).
- **WYMAGANY `jarvis restart`** żeby żywy daemon dostał: auto-run, jedną konwersację głosu, mowę [[GŁOS]], nowe API i panel.

## Pliki niezacommitowane
`jarvis/daemon/app.py`, `jarvis/tools/permissions.py`, `jarvis/brain/context_builder.py`, `jarvis/brain/speech_text.py`, `jarvis/brain/claude_cli_adapter.py`, `jarvis/voice/speech_form_stream.py` (nowy), `tests/*` (test_tool_permissions, test_effective_tool_policy [nowy], test_tool_result_continuation, test_context_builder, test_speech_text_redaction [nowy], test_speech_form_stream [nowy]).
⚠️ `jarvis/panel/assets/app.js` ma zmiany, które NIE są moje (Ozzy/inna sesja) — nie zagarniać.

## 🧠 MÓZG: fix persony + tempo (wieczór, po live-teście Ozzy'ego)
Objaw na żywo: „proste pytanie po minucie" + model odpowiadał „to ja, Claude Code w terminalu, ktoś wkleił mi prompt Jarvisa" i wmawiał brak narzędzi. Rozbiórka: 31 s generacji (opus-4-8 + zimny spawn CLI + ~37k kontekstu) + TTS czytał 1400 znaków markdownu (model olał [[GŁOS]]); wypadnięcie z roli bo persona szła jako INPUT, a globalny CLAUDE.md Ozzy'ego („nie importuj starych założeń Jarvisa") wczytywał się do sesji mózgu i przekonywał model, że to stara wklejka.
- **Fix 1 — prawdziwy system prompt**: `format_cli_system_prompt` (tożsamość „live Jarvis runtime, not a pasted transcript" + persona + pamięć + narzędzia) idzie przez `--system-prompt`; stdin (`format_cli_user_prompt`) niesie tylko rozmowę. `--setting-sources ""` odcina sesję mózgu od ustawień/CLAUDE.md operatora. Oba flagi w `_ALLOWED_CLI_FLAGS`; `apply_claude_system_prompt` w obu torach (blocking `use_system_prompt=True` + stream); codex/warm zostają na single-blob `format_cli_prompt` (bez regresu). **Zweryfikowane na żywo**: realny `claude -p` z personą Jarvisa → odpowiada w charakterze, zero „Claude Code".
- **Fix 2 — sonnet w pickerze**: `_PINNED_MODELS = ("claude-sonnet-4-6",)` doklejane do KAŻDEGO wyniku discovery (model-lista z discovery pomijała starsze id mimo że CLI je przyjmuje — sprawdzone na żywo `S46-OK`). Po restarcie panel pokaże sonnet-4-6.
- **Fix 3 — first-sound bez bloku**: `SpeechFormStreamRouter` dostał `_PASSTHROUGH` (próg 64 znaki): model bez [[GŁOS]] → surowy live-stream (markery i tak wycinane), zamiast ciszy do finalize; krótkie odpowiedzi pod progiem dalej mówi finalize (nic nie leci 2×).
- **Warm adapter NIE włączony świadomie**: użyłby single-blob promptu = wróciłby persona-break. Przed włączeniem trzeba nauczyć warm tor `--system-prompt` (sesja startowa) — TODO.
- Rekomendacja dla Ozzy'ego: model `claude-sonnet-4-6` + effort low dla głosu (po restarcie dostępny w pickerze).
- Testy: adaptery/modele/speech/pipeline 219 zielonych; zaktualizowane kontrakty: `test_claude_adapter_argv_uses_first_class_contract_fields` (+2 flagi), `test_fake_claude_cli_turn_uses_selected_adapter_model_metadata`, `test_prompt_formatter_is_a_bare_header...` (tożsamość wróciła świadomie), test_claude_models (pinned tail).

## Następne kroki
1. **Ozzy potwierdza na żywo:** 3 PTT pod rząd (⌘+⇧ trzymaj/mów/puść) → wszystkie w JEDNEJ konwersacji, też te anulowane. Log: `grep "voice-turn conversation" ~/.jarvis/logs/jarvisd*.log`. **Restart daemona wymagany** (mowa `[[GŁOS]]` jest teraz aktywna) — przy okazji sprawdzić na ucho, że TTS mówi krótką formę, nie markdown.
2. **Decyzja Ozzy'ego: review Important #2** (zakres overlay — patrz sekcja review) → potem #1 (sync kontraktu routes_runtime) i #3 (serializacja kontynuacji auto-run).
3. Rozważyć: panel/tekst też domyślnie na tę samą trwałą konwersację (Ozzy chce „jedna konwersacja i chuj"). Panel ma dropdown historii — decyzja UX.
4. UX: przelotny „pending approval" błyska w panelu zanim auto-zatwierdzi — do wygładzenia (nie blokuje; review Minor #8 to to samo).
5. Commit dopiero na wyraźną prośbę Ozzy'ego.

## Pełna regresja
`.venv/bin/python -m pytest tests/test_tool_result_continuation.py tests/test_effective_tool_policy.py tests/test_tool_permissions.py tests/test_context_builder.py tests/test_speech_text_redaction.py tests/test_speech_form_stream.py tests/test_model_tool_permission_policy.py tests/test_streaming_turn_speech.py -q`
(5 czerwonych w test_api_smoke.py to **pre-existing**, nie z tej sesji — potwierdzone `git stash`.)
